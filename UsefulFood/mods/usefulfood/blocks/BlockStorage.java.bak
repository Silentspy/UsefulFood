package usefulfood.blocks;

import java.util.ArrayList;
import java.util.List;
import java.util.Random;

import usefulfood.*;
import usefulfood.items.ItemBlockStorage;

import cpw.mods.fml.common.registry.GameRegistry;
import cpw.mods.fml.relauncher.Side;
import cpw.mods.fml.relauncher.SideOnly;
import net.minecraft.block.*;
import net.minecraft.block.material.Material;
import net.minecraft.creativetab.CreativeTabs;
import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;
import net.minecraft.util.Icon;

public class BlockStorage extends Block
{
    private Icon cakeTopIcon;
    private Icon cakeBottomIcon;
    private Icon field_94382_c;
	
	
	public BlockStorage(int var1, String name) {
		super(var1, Material.cloth);
		this.setCreativeTab(mod_usefulfood.tabUsefulFood);
		this.setBurnProperties(var1, 50, 100);
		this.setHardness(0.5F);
		this.setStepSound(soundClothFootstep);
		GameRegistry.registerBlock(this, ItemBlockStorage.class, name);
	}

	public Icon getIcon(int side, int metadata) {
		switch(metadata) 
		 {
	   		case 0: 
	   		{
	   			return side != 1 && side != 0 ? cakeTopIcon : cakeTopIcon;
	   		}
	   		case 1: 
	   		{ 
	   			return side != 1 && side != 0 ? cakeTopIcon : cakeTopIcon;
	   		}
	   		case 2: 
	   		{ 
	   			return side != 1 && side != 0 ? cakeTopIcon : cakeTopIcon;
	   		}
	   		case 3: 
	   		{ 
	   			return side != 1 && side != 0 ? cakeTopIcon : cakeTopIcon;
	   		}
	   		case 4: 
	   		{ 
	   			return side != 1 && side != 0 ? cakeTopIcon : cakeTopIcon;
	   		}
		 }
		return cakeTopIcon;
	}

	public void addCreativeItems(ArrayList itemList) {
		itemList.add(new ItemStack(this, 1));
	}

	public int quantityDropped(Random par1Random) {
		return 9;
	}
    
    public int idDropped(int par1, Random par2Random, int par3)
    {
    	switch(par1) 
		 {
	   		case 0: 
	   		{
	   			return Item.wheat.itemID;
	   		}
	   		case 1: 
	   		{ 
	   		    return Item.reed.itemID;
	   		}
		 }
		return Item.wheat.itemID;
    }
	
	@SideOnly(Side.CLIENT)
    public void getSubBlocks(int par1, CreativeTabs par2CreativeTabs, List par3List)
    {
        for (int var4 = 0; var4 < 2; ++var4)
        {
            par3List.add(new ItemStack(par1, 1, var4));
        }
    }
}
